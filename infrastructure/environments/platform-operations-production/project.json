{
	"name": "terraform-platform-operations-production",
	"projectType": "application",
	"root": "infrastructure/environments/platform-operations-production",
	"implicitDependencies": ["terraform-modules"],
	"targets": {
		"terraform:validate": {
			"executor": "nx:run-commands",
			"cache": true,
			"inputs": ["default", "^default"],
			"options": {
				"command": "terraform fmt -check -recursive infrastructure/environments/platform-operations-production && terraform -chdir=infrastructure/environments/platform-operations-production init -reconfigure -backend=false -input=false -lockfile=readonly && terraform -chdir=infrastructure/environments/platform-operations-production validate",
				"cwd": "."
			}
		}
	},
	"tags": ["scope:infrastructure", "type:terraform", "domain:platform"]
}
