{
	"name": "terraform-trust-risk-production",
	"projectType": "application",
	"root": "infrastructure/environments/trust-risk-production",
	"implicitDependencies": ["terraform-modules"],
	"targets": {
		"terraform:validate": {
			"executor": "nx:run-commands",
			"cache": true,
			"inputs": ["default", "^default"],
			"options": {
				"command": "terraform fmt -check -recursive infrastructure/environments/trust-risk-production && terraform -chdir=infrastructure/environments/trust-risk-production init -reconfigure -backend=false -input=false -lockfile=readonly && terraform -chdir=infrastructure/environments/trust-risk-production validate",
				"cwd": "."
			}
		}
	},
	"tags": ["scope:infrastructure", "type:terraform", "domain:trust-risk"]
}
