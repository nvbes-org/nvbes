# Rust OSS Adapters

Target location for OSS provider adapters such as MinIO, SMTP, Lago, NATS and OpenSearch.

Adapters here must implement stable product interfaces without depending on managed cloud providers.
