{
  "name": "billing",
  "projectType": "library",
  "root": "libs/rust/billing",
  "sourceRoot": "libs/rust/billing/src",
  "tags": ["type:lib", "domain:shared", "layer:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build --manifest-path libs/rust/billing/Cargo.toml"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo check --manifest-path libs/rust/billing/Cargo.toml"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test --manifest-path libs/rust/billing/Cargo.toml"
      }
    }
  }
}
