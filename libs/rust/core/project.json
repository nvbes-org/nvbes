{
  "name": "core",
  "projectType": "library",
  "root": "libs/rust/core",
  "sourceRoot": "libs/rust/core/src",
  "tags": ["type:lib", "domain:shared", "layer:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build --manifest-path libs/rust/core/Cargo.toml"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo check --manifest-path libs/rust/core/Cargo.toml"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test --manifest-path libs/rust/core/Cargo.toml"
      }
    }
  }
}
