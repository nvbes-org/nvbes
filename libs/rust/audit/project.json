{
  "name": "audit",
  "projectType": "library",
  "root": "libs/rust/audit",
  "sourceRoot": "libs/rust/audit/src",
  "tags": ["type:lib", "domain:shared", "layer:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build --manifest-path libs/rust/audit/Cargo.toml"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo check --manifest-path libs/rust/audit/Cargo.toml"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test --manifest-path libs/rust/audit/Cargo.toml"
      }
    }
  }
}
