{
  "name": "tenancy",
  "projectType": "library",
  "root": "libs/rust/tenancy",
  "sourceRoot": "libs/rust/tenancy/src",
  "tags": ["type:lib", "domain:shared", "layer:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build --manifest-path libs/rust/tenancy/Cargo.toml"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo check --manifest-path libs/rust/tenancy/Cargo.toml"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test --manifest-path libs/rust/tenancy/Cargo.toml"
      }
    }
  }
}
