# Rust Cloud Operations

Status: future Cloud capability, outside the active V1 foundation. This crate
must not become an active dependency until the first product is selected and
passes the global FinOps gate.

Target location for nvbes Cloud provisioning, metering, SLA and operations logic.
