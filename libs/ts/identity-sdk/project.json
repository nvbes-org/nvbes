{
  "name": "identity-sdk",
  "projectType": "library",
  "root": "libs/ts/identity-sdk",
  "sourceRoot": "libs/ts/identity-sdk/src",
  "tags": ["type:lib", "domain:shared", "layer:sdk"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir libs/ts/identity-sdk build"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir libs/ts/identity-sdk dev"
      }
    }
  }
}
