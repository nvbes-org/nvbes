{
  "name": "@nvbes/identity-sdk",
  "version": "0.1.0",
  "description": "SDK TypeScript pour intégrer nvbes Identity (OAuth2)",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --tsconfig tsconfig.json",
    "check": "pnpm run typecheck",
    "dev": "tsup src/index.ts --format cjs,esm --dts --tsconfig tsconfig.json --watch",
    "format:check": "vp fmt --check src package.json tsconfig.json vite.config.ts",
    "lint": "vp lint src",
    "typecheck": "vp check src package.json tsconfig.json vite.config.ts"
  },
  "dependencies": {
    "@nvbes/http-client": "workspace:*"
  },
  "devDependencies": {
    "tsup": "catalog:",
    "typescript": "catalog:"
  }
}
