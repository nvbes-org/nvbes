# @nvbes/backoffice-service-sdk-core

Status: archived Backoffice contract retained as inventory. Active V1 internal
operations use the minimal Platform Operations boundary and must not depend on
this package by default.

OpenAPI contract and generated TypeScript types for the private nvbes Backoffice API.

```bash
pnpm generate:ts
```

The generated `src/types.gen.ts` file is produced from `openapi.json`.
