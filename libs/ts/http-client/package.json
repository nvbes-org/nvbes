{
  "name": "@nvbes/http-client",
  "version": "0.1.0",
  "description": "Shared fetch transport with runtime DTO validation",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "module": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "scripts": {
    "build": "vp pack",
    "check": "pnpm run typecheck",
    "dev": "vp pack --watch",
    "format:check": "vp fmt --check src package.json tsconfig.json",
    "lint": "vp lint src",
    "typecheck": "tsc --pretty false --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
