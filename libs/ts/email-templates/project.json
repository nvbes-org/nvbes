{
  "name": "email-templates",
  "projectType": "library",
  "root": "libs/ts/email-templates",
  "sourceRoot": "libs/ts/email-templates/src",
  "tags": ["type:lib", "domain:shared", "layer:ui"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir libs/ts/email-templates build"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir libs/ts/email-templates check"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir libs/ts/email-templates lint"
      }
    }
  }
}
