{
  "name": "identity-client",
  "projectType": "library",
  "root": "libs/ts/identity-client",
  "sourceRoot": "libs/ts/identity-client/src",
  "tags": ["type:lib", "domain:identity", "layer:client"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir libs/ts/identity-client build"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir libs/ts/identity-client dev"
      }
    }
  }
}
