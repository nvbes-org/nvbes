# Codex Implement Feature Prompt

Implement the requested scoped feature in nvbes.

Required behavior:

- Read `AGENTS.md`.
- Select profile, workflow, and skills from `.agents/registry.yaml`.
- Follow existing local patterns.
- Keep changes scoped.
- Run required validation.
- Return changed files, validation, and residual risks.
