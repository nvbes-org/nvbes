# Rust Workspace Tools

Intégration entre le Cargo Workspace et Nx pour l'exécution unifiée des cibles `check`, `lint`, `test` et `mutation`.

- `check-mutation.mjs`: gate de mutation testing (`check-mutation.test.mjs`).
