# Cloud Web

Cloud web owns the Cloud product surface for workspaces, Drive and completed
Cloud product modules.

It uses Account for identity entry points and consumes Cloud API contracts for
product state. It does not host provider-specific provisioning adapters.

provisioning plans may be surfaced here for users, while plan/apply execution
remains outside the frontend runtime.
