# @nvbes/billing-client

Client TypeScript typé pour l'interaction avec l'API Billing nvbes.

## Utilisation

```typescript
import { createBillingClient } from '@nvbes/billing-client';

const client = createBillingClient({
  baseUrl: 'https://api.nvbes.local',
});
```
