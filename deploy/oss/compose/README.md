# Docker Compose

This directory is reserved for the public Docker Compose installer.

The Compose stack should use only OSS services and released nvbes container images. It must not require managed cloud services or proprietary providers.
