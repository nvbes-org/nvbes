# Internal Deployments

This directory is reserved for V1 Platform Operations and tightly scoped
internal tooling. No permanent internal runtime or paid tool is deployed until
a measured need fits the EUR 30 total recurring budget. The solo operator uses
safe, audited manual procedures by default.
