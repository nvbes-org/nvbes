# Convention Memory

- Use `rtk` before shell commands.
- Use `rg` for file and text discovery.
- Use `apply_patch` for manual edits.
- Keep files short: consider extraction near 300 lines, require it near 500.
- Avoid `utils`, `helpers`, and generic catch-all modules.
- Do not hide business logic in barrels.
