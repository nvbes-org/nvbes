{
  "name": "identity-worker",
  "projectType": "application",
  "root": "apps/identity-worker",
  "sourceRoot": "apps/identity-worker/src",
  "tags": ["type:app", "domain:identity", "layer:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build --manifest-path apps/identity-worker/Cargo.toml"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo check --manifest-path apps/identity-worker/Cargo.toml"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test --manifest-path apps/identity-worker/Cargo.toml"
      }
    }
  }
}
