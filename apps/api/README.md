# API Apps

Public REST APIs and gateway bootstraps live here when they are promoted into
the target monorepo layout. Product logic remains in `libs/*/products`; this
directory is for runtime composition only.
