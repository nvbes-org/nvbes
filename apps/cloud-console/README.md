# Cloud Console

Cloud-only operations console boundary.

The console surfaces provisioning plans, apply status, rollback status, SLA
state, support signals and operational evidence for managed nvbes workspaces.
It consumes Cloud Control API contracts and must not embed provisioning domain
rules.

This area is Cloud-only and must not be exported to `nvbes-oss`.
