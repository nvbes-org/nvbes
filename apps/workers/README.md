# Worker Apps

Durable product workers and background job runtimes live here in the target
layout. Workers must own their domain queues explicitly and keep replay and
idempotency behavior covered by tests.
